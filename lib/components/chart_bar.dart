import 'package:flutter/material.dart';

class ChatBar extends StatelessWidget {
  /*final String day;
  final double value;

  ChatBar(this.day, this.value)
  */
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
